{% extends 'base.html.twig' %}

{% block title %}CommentaireHabitat{% endblock %}

{% block body %}
    <h1>CommentaireHabitat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire_habitat.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ commentaire_habitat.commentaire }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ commentaire_habitat.date ? commentaire_habitat.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_habitat_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_habitat_edit', {'id': commentaire_habitat.id}) }}">edit</a>

    {{ include('commentaire_habitat/_delete_form.html.twig') }}
{% endblock %}
